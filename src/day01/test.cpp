#include <catch2/catch_test_macros.hpp>

#include "solution.hpp"

/*
TEST_CASE("Basic arithmetic operations", "[arithmetic]") {
  REQUIRE(add(2, 3) == 5);
  REQUIRE(subtract(5, 3) == 2);
  REQUIRE(multiply(4, 3) == 12);
  REQUIRE(divide(10, 2) != nullptr);
  REQUIRE(*divide(10, 2) == 5);
  REQUIRE(divide(10, 0) == nullptr);
}
*/
